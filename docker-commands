docker info
docker ps 
docker ps -a
docker images ls
docker container ls
docker pull image_naem
docker rmi image_name/image_id 
docker run iamge_name
docker login -> push or pull images private
docker pause id // container pause
docker unpause id //container unpause
docker restart id 
docker stop id
docker kill id
docker run ubuntu shell_cmds(bash shell) like whoami hostname
docker run -it ubuntu bash //interactive run
docker attach id // attach to last cmd and entrypoint
docker stats id / blank
docker inspect id
docker run -d centos ping localhost
docker logs -f id
docker system prune -a -f
docker exec -it c_id bash /login to new termial
docker rename c_id vishal
docker run -itd --name vishal ubuntu bash
docker commit c_id image_name:version
docker push username/iamge_name:version
docker run --itd -p 80:80 nginx 
docker cp file c_id:/path
docker volume ls
docker volume create vol_name
docker run -itd -v vol_name:/path --name test ubuntu
docker volume rm vol_name
docker build -t name_image .

docker network ls
docker network create net_name
docker network rm net_name
docker netework connect net_name c_id
docker network disconnect net_name c_id

docker save mlopsimage:latest -o mlopsimage.tar
ctr -n k8s.io images import mlopsimage.tar
crictl images // import images to k8 cluster


docker network inspect net_name
docker network prune -f

docker run -itd --name web --network net_name nginx

