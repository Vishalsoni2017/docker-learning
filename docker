docker

tradational arch vs virtulzation

tradational arch -> hardware -> os -> applications 
virtualaztion -> hardware -> virutal layer (hyperisor) -> VM (os + appilcation)

hypervisor 
    type 1 bare metal- h/w -> hypervisor -> vm (os+appilcation) e.g cloud platform 
    type 2 Hosted -  h/w -> host os -> hypervisor -> vm (guest os+ applcication) e.g vmware

Containerization 
is a lightweight virtualization method where applications run in isolated environments called containers, sharing the host OS kernel while maintaining their own dependencies and configurations.

h/w -> hosted os ->container enginer (docker engine) -> container (lightweight os + app+ library)

if app stops deleted container will also stops.
-> give resource to h/w ->bootup time low

Installation -- install-docker.sh

docker 
    opensource , isolated container creatation , c loosely isolated loosely couple


### Docker Architecture
docker host -> ubuntu - ec2 instance

Docker architecture is based on a client-server model. It consists of the following components:

1. **Docker Client**: The *interface through which users interact with Docker*. Commands like `docker run` are sent from the client to the Docker daemon.

2. **Docker Daemon (dockerd)**: The *core service that manages Docker objects* such as images, containers, networks, and volumes. It listens for API requests and performs the requested actions.

3. **Docker Images**: *Read-only templates* used to create containers. They contain the application code, dependencies, and runtime environment.

4. **Docker Containers**: *Lightweight, portable, and isolated environments* created from Docker images. They run the application and its dependencies.

5. **Docker Registry**: A storage and distribution system for Docker images. Public registries like Docker Hub or private registries can be used to store and share images.

6. **Docker Engine**: The *runtime* that enables containerization. It includes the Docker Daemon, REST API, and CLI tools.

In simple terms, Docker allows you to package applications and their dependencies into containers, ensuring consistency across different environments.


path 

/var/lib/docker -> all files path volume, network, images

user

usermod -aG docker $USER / ubuntu
newgrp docker


Lifecycle

create -> running -> kill
                -> stop
                -> pause

exist
CTL+P+Q exit without close
CTL d to close

OWN Image
1.Base Image - docker commit c_id image_name:version 
2. Dokcerfile

Docker file

FROM -> base image used 
RUN -> cmd to exec at crating image
MAINTAINER -> author , descr
COPY -> copy from host to images
ADD -> extact tar to images
EXPOSE -> port to expose
WORKDIR -> wokring dir
ENV -> set envir varible
CMD -> cmd will run in container when create or runing , can overwritten
ENTRYPOINT -> same cmd but has high prioirty , can overrwrite
ARG	->Use build-time variables.
HEALTHCHECK	->Check a container's health on startup.
LABEL	->Add metadata to an image.
ONBUILD	->Specify instructions for when the image is used in a build.
SHELL	->Set the default shell of an image.
STOPSIGNAL	->Specify the system call signal for exiting a container.
USER	->Set user and group ID.
VOLUME	->Create volume mounts.

Build Layer by Layer to avoid building images from stratch for new images by using caches


can create .dockerignore like git
use USER to avoid root 


